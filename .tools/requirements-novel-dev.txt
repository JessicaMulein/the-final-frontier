# Development test dependencies for the objective manuscript checker.
#
# Created by task 7.1 of the `The-Final-Frontier-novel` spec, which is the first
# task permitted to add a dependency manifest. Authority: design "Objective Test
# Stack" and Requirements 12.1, 12.14, 12.15.
#
# Interpreter these pins were selected against: CPython 3.9.6 (system Python; no
# virtualenv). All test and checker code must stay 3.9-compatible.
#
# Dev-only. `.tools/check_novel.py` runs on the standard library alone and needs
# nothing from this file; only the test suite under `.tools/tests/` does.
#
# Exact pins only, as the design requires: no open ranges, no compatible-release
# specifiers, no unpinned names. Every requirement line below uses `==`.
#
# Transitive dependencies are intentionally left unpinned. This is a local dev
# manifest, not a reproducible lock file, and no lock file is claimed or
# provided.
pytest==8.4.2
hypothesis==6.141.1
